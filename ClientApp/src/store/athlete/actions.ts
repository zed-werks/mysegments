import { ActionTree } from 'vuex';
import axios from 'axios';
import { RootState } from '../types';
import AthleteProfile from '@/models/AthleteProfile';

export const actions: ActionTree<AthleteProfile, RootState> = {
};
